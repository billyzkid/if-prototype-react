{
  "name": "if-prototype-react-server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.14.1"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-latest": "^6.22.0",
    "nodemon": "^1.11.0"
  },
  "scripts": {
    "start": "nodemon ./src --exec babel-node --presets latest",
    "build": "babel ./src -d ./build --presets latest",
    "serve": "node ./build"
  }
}